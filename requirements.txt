# Core web app
streamlit==1.30.0
pandas>=1.5.0
matplotlib>=3.5.0
requests>=2.28.0

# Time-series forecasting
prophet==1.2.1

# Finance / data
yahooquery>=2.2.0
numpy>=1.24.0

# ML / utils
scikit-learn>=1.2.0
joblib>=1.2.0

# LangChain, RAG and embeddings
langchain>=0.0.285
langchain-community @ git+https://github.com/hwchase17/langchain-extensions.git@main

# Sentence-transformers + HuggingFace embeddings
sentence-transformers>=2.2.2
transformers>=4.30.0

# FAISS (CPU) - if using GPU change to faiss-gpu
faiss-cpu==1.7.4

# Groq / LLM client (used in app as `groq`)
groq==0.4.3

# TTS / translation
gTTS>=2.4.0
deep-translator>=1.9.6

# Other helpers
python-dotenv>=1.0.0
openpyxl>=3.1.0

# Optional / helper libs often required in cloud deployments
uvicorn>=0.22.0
gunicorn>=20.1.0
